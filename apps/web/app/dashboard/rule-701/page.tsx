import WorkInProgress from "@/components/WorkInProgress";

export const metadata = {
  title: "Rule 701",
};

const Rule701Page = () => {
  return (
    <div>
      <WorkInProgress />
    </div>
  );
};

export default Rule701Page;
